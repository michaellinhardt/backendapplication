export const

	// Returns a new string with the first character converted to lower case.
	lowerFirstChar = string => string.charAt(0).toLowerCase() + string.slice(1),
	// Returns a new string with the first character converted to upper case.
	upperFirstChar = string => string.charAt(0).toUpperCase() + string.slice(1)
